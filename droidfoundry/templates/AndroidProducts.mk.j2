# Generated by DroidFoundry

PRODUCT_MAKEFILES := \
    $(LOCAL_DIR)/{{ rom }}_{{ device }}.mk

COMMON_LUNCH_CHOICES := \
    {{ rom }}_{{ device }}-user \
    {{ rom }}_{{ device }}-userdebug \
    {{ rom }}_{{ device }}-eng
